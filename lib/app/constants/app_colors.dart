import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const primary = Color(0xFFFF9800);
  static const appBarBg = Color(0xFFE9E2EF);

  // User interface colors
  static const secondaryText = Color(0xFFA6A0A7);
  static const likeInactive = Color(0xFF686868);
  static const likeActive = Color(0xFFFF3B30);
}